export default function Sparkline({values=[]}){if(!values.length)return null;const w=120,h=28,max=Math.max(...values,1),min=Math.min(...values,0),range=max-min||1;const points=values.map((v,i)=>`${(i/(values.length-1||1))*w},${h-((v-min)/range)*h}`).join(' ');return <svg viewBox={`0 0 ${w} ${h}`} className='h-7 w-full'><polyline fill='none' stroke='#6c63ff' strokeWidth='2' points={points}/></svg>}
